{
  "name": "openai-nim-proxy",
  "version": "2.0.0",
  "description": "OpenAI compatible proxy for NVIDIA NIM API (Chat + Images)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.18.2",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": "24.x"
  }
}
